[build]
# No build step for static HTML/CSS
command = ""
publish = "."

[dev]
port = 8888

[headers]
# Basic caching for static assets
[[headers.values]]
for = "/images/*"
[headers.values.headers]
Cache-Control = "public, max-age=31536000, immutable"
